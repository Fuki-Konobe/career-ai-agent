services:
  gakuchika-mentor:
    build: .
    container_name: gakuchika_ui
    ports:
      - "8502:8501"
    volumes:
      - .:/app
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - PYTHONIOENCODING=utf-8:replace
      - PYTHONLEGACYWINDOWSSTDIO=0  # Windows環境の場合に有効
    tty: true